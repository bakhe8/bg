"\"\"\"\nPlaceholder for the upcoming normalization pipeline.\n\nThis module will progressively absorb logic from\n``excel_to_json_converter`` (normalize_value, bank lookups, etc.). The\ncurrent stub keeps the public surface ready for tests.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any, Dict\n\n\ndef normalize_record(record: Dict[str, Any]) -> Dict[str, Any]:\n    \"\"\"Return the record as-is for now.\n\n    Once the refactor reaches the normalization step this function will\n    contain the actual transformations.\n    \"\"\"\n\n    return dict(record)\n\n\n__all__ = [\"normalize_record\"]\n*** End Patch"/>
